apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Aggregate: lets you `kubectl apply -k overlays/dev` to deploy both
# services at once. Argo CD's per-service Applications point at
# overlays/dev/backend and overlays/dev/frontend directly.
resources:
  - namespace.yaml
  - backend
  - frontend
